{
  "name": "microservice-chain-demo",
  "version": "0.1.0",
  "private": true,
  "description": "PIC Microservice Chain Demo - Authority propagation through Gateway -> Archive -> Storage",
  "type": "module",
  "scripts": {
    "build": "npm run build:gateway && npm run build:archive && npm run build:storage",
    "build:gateway": "cd gateway && npm run build",
    "build:archive": "cd archive && npm run build",
    "build:storage": "cd storage && npm run build",
    "demo": "bash demo.sh",
    "install:all": "npm install && cd gateway && npm install && cd ../archive && npm install && cd ../storage && npm install"
  },
  "workspaces": [
    "gateway",
    "archive",
    "storage"
  ]
}
